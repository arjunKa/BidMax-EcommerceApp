CREATE TABLE items (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    name TEXT,
    price DECIMAL(10,2),
    type TEXT,
    endtime DATETIME,
    description TEXT,
    shipping DECIMAL(10,2)
);
    -- Add other columns as needed